# Векторы, матрицы и операции

> Любая нейронная сеть — это всего лишь умножение матриц с дополнительными шагами.

**Тип:** Build
**Языки:** Python, Julia
**Предварительные требования:** Этап 1, урок 01 (Интуитивное понимание линейной алгебры)
**Время:** ~60 минут

## Цели обучения

- Создать класс Matrix с поэлементными операциями, умножением матриц, транспонированием, определителем и обратной матрицей
- Различать поэлементное умножение и умножение матриц и объяснять, когда применяется каждое из них
- Реализовать один полносвязный слой нейронной сети (`relu(W @ x + b)`), используя только созданный с нуля класс Matrix
- Объяснить правила транслирования и то, как сложение со смещением работает во фреймворках нейронных сетей

## Задача

Вы хотите создать нейронную сеть. Читаете код и видите следующее:

```
output = activation(weights @ input + bias)
```

Оператор `@` означает умножение матриц. `weights` — матрица. `input` — вектор. Если вы не знаете, что делают эти операции, строка выглядит как магия. Если знаете, то перед вами весь прямой проход слоя, выраженный тремя операциями.

Каждое изображение, обрабатываемое моделью, — матрица значений пикселей. Каждый эмбеддинг слова — вектор. Каждый слой любой нейронной сети — матричное преобразование. Нельзя создавать системы ИИ, не владея матричными операциями так же свободно, как нельзя писать код, не понимая переменных.

В этом уроке мы с нуля выработаем такую свободу.

## Основная идея

### Векторы: упорядоченные списки чисел

Вектор — это список чисел, обладающий направлением и длиной. В ИИ векторы представляют точки данных, признаки или параметры.

```
v = [3, 4]        -- a 2D vector
w = [1, 0, -2]    -- a 3D vector
```

Двумерный вектор `[3, 4]` указывает на координаты (3, 4) на плоскости. Его длина равна 5 (треугольник со сторонами 3, 4 и 5).

### Матрицы: таблицы чисел

Матрица — двумерная таблица со строками и столбцами. Матрица m x n содержит m строк и n столбцов.

```
A = | 1  2  3 |     -- 2x3 matrix (2 rows, 3 columns)
    | 4  5  6 |
```

В нейронных сетях матрицы весов преобразуют входные векторы в выходные. Слой с 784 входами и 128 выходами использует матрицу весов размером 128x784.

### Почему важны размерности

Для умножения матриц действует строгое правило: `(m x n) @ (n x p) = (m x p)`. Внутренние размерности должны совпадать.

```
(128 x 784) @ (784 x 1) = (128 x 1)
  weights       input       output

Inner dimensions: 784 = 784  -- valid
```

Именно из-за этого в PyTorch возникает ошибка несовпадения размерностей.

### Карта операций

| Операция | Что она делает | Применение в нейронной сети |
|-----------|-------------|-------------------|
| Сложение | Поэлементно объединяет | Добавление смещения к выходу |
| Умножение на скаляр | Масштабирует каждый элемент | Скорость обучения * градиенты |
| Умножение матриц | Преобразует векторы | Прямой проход слоя |
| Транспонирование | Меняет строки и столбцы местами | Обратное распространение ошибки |
| Определитель | Даёт одно итоговое число | Проверка обратимости |
| Обратная матрица | Обращает преобразование | Решение линейных систем |
| Единичная матрица | Ничего не меняет | Инициализация, остаточные связи |

### Поэлементное умножение и умножение матриц

Новички постоянно путают эти операции.

Поэлементное умножение: перемножаются элементы в соответствующих позициях. Обе матрицы должны иметь одинаковую размерность.

```
| 1  2 |   | 5  6 |   | 5  12 |
| 3  4 | * | 7  8 | = | 21 32 |
```

Умножение матриц: вычисляются скалярные произведения строк и столбцов. Внутренние размерности должны совпадать.

```
| 1  2 |   | 5  6 |   | 1*5+2*7  1*6+2*8 |   | 19  22 |
| 3  4 | @ | 7  8 | = | 3*5+4*7  3*6+4*8 | = | 43  50 |
```

Это разные операции с разными результатами и разными правилами.

### Транслирование

Когда к матрице выходов прибавляется вектор смещения, размерности не совпадают. Транслирование растягивает меньший массив до подходящего размера.

```
| 1  2  3 |   +   [10, 20, 30]
| 4  5  6 |

Broadcasting stretches the vector across rows:

| 1  2  3 |   | 10  20  30 |   | 11  22  33 |
| 4  5  6 | + | 10  20  30 | = | 14  25  36 |
```

Каждый современный фреймворк делает это автоматически. Понимание этого механизма помогает избежать недоумения, когда размерности кажутся неверными, но код работает.

```figure
vector-projection
```

## Создаём самостоятельно

### Шаг 1. Класс Vector

```python
class Vector:
    def __init__(self, data):
        self.data = list(data)
        self.size = len(self.data)

    def __repr__(self):
        return f"Vector({self.data})"

    def __add__(self, other):
        return Vector([a + b for a, b in zip(self.data, other.data)])

    def __sub__(self, other):
        return Vector([a - b for a, b in zip(self.data, other.data)])

    def __mul__(self, scalar):
        return Vector([x * scalar for x in self.data])

    def dot(self, other):
        return sum(a * b for a, b in zip(self.data, other.data))

    def magnitude(self):
        return sum(x ** 2 for x in self.data) ** 0.5
```

### Шаг 2. Класс Matrix с основными операциями

```python
class Matrix:
    def __init__(self, data):
        self.data = [list(row) for row in data]
        self.rows = len(self.data)
        self.cols = len(self.data[0])
        self.shape = (self.rows, self.cols)

    def __repr__(self):
        rows_str = "\n  ".join(str(row) for row in self.data)
        return f"Matrix({self.shape}):\n  {rows_str}"

    def __add__(self, other):
        return Matrix([
            [self.data[i][j] + other.data[i][j] for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def __sub__(self, other):
        return Matrix([
            [self.data[i][j] - other.data[i][j] for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def scalar_multiply(self, scalar):
        return Matrix([
            [self.data[i][j] * scalar for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def element_wise_multiply(self, other):
        return Matrix([
            [self.data[i][j] * other.data[i][j] for j in range(self.cols)]
            for i in range(self.rows)
        ])

    def matmul(self, other):
        return Matrix([
            [
                sum(self.data[i][k] * other.data[k][j] for k in range(self.cols))
                for j in range(other.cols)
            ]
            for i in range(self.rows)
        ])

    def transpose(self):
        return Matrix([
            [self.data[j][i] for j in range(self.rows)]
            for i in range(self.cols)
        ])

    def determinant(self):
        if self.shape == (1, 1):
            return self.data[0][0]
        if self.shape == (2, 2):
            return self.data[0][0] * self.data[1][1] - self.data[0][1] * self.data[1][0]
        det = 0
        for j in range(self.cols):
            minor = Matrix([
                [self.data[i][k] for k in range(self.cols) if k != j]
                for i in range(1, self.rows)
            ])
            det += ((-1) ** j) * self.data[0][j] * minor.determinant()
        return det

    def inverse_2x2(self):
        det = self.determinant()
        if det == 0:
            raise ValueError("Matrix is singular, no inverse exists")
        return Matrix([
            [self.data[1][1] / det, -self.data[0][1] / det],
            [-self.data[1][0] / det, self.data[0][0] / det]
        ])

    @staticmethod
    def identity(n):
        return Matrix([
            [1 if i == j else 0 for j in range(n)]
            for i in range(n)
        ])
```

### Шаг 3. Проверка в работе

```python
A = Matrix([[1, 2], [3, 4]])
B = Matrix([[5, 6], [7, 8]])

print("A + B =", (A + B).data)
print("A @ B =", A.matmul(B).data)
print("A^T =", A.transpose().data)
print("det(A) =", A.determinant())
print("A^-1 =", A.inverse_2x2().data)

I = Matrix.identity(2)
print("A @ A^-1 =", A.matmul(A.inverse_2x2()).data)
```

### Шаг 4. Связь с нейронными сетями

```python
import random

inputs = Matrix([[0.5], [0.8], [0.2]])
weights = Matrix([
    [random.uniform(-1, 1) for _ in range(3)]
    for _ in range(2)
])
bias = Matrix([[0.1], [0.1]])

def relu_matrix(m):
    return Matrix([[max(0, val) for val in row] for row in m.data])

pre_activation = weights.matmul(inputs) + bias
output = relu_matrix(pre_activation)

print(f"Input shape: {inputs.shape}")
print(f"Weight shape: {weights.shape}")
print(f"Output shape: {output.shape}")
print(f"Output: {output.data}")
```

Это один полносвязный слой: `output = relu(W @ x + b)`. Каждый полносвязный слой любой нейронной сети делает именно это.

## Используем готовое

NumPy делает всё описанное выше меньшим числом строк и на порядки быстрее.

```python
import numpy as np

A = np.array([[1, 2], [3, 4]])
B = np.array([[5, 6], [7, 8]])

print("A + B =\n", A + B)
print("A * B (element-wise) =\n", A * B)
print("A @ B (matrix multiply) =\n", A @ B)
print("A^T =\n", A.T)
print("det(A) =", np.linalg.det(A))
print("A^-1 =\n", np.linalg.inv(A))
print("I =\n", np.eye(2))

inputs = np.random.randn(3, 1)
weights = np.random.randn(2, 3)
bias = np.array([[0.1], [0.1]])
output = np.maximum(0, weights @ inputs + bias)

print(f"\nNeural network layer: {weights.shape} @ {inputs.shape} = {output.shape}")
print(f"Output:\n{output}")
```

Оператор `@` в Python вызывает `__matmul__`. NumPy реализует его с помощью оптимизированных процедур BLAS, написанных на C и Fortran. Та же математика, но в 100x раз быстрее.

Транслирование в NumPy:

```python
matrix = np.array([[1, 2, 3], [4, 5, 6]])
bias = np.array([10, 20, 30])
print(matrix + bias)
```

NumPy автоматически транслирует одномерное смещение на обе строки. Именно так сложение со смещением работает в каждом фреймворке нейронных сетей.

## Результат

Этот урок создаёт промпт для обучения матричным операциям через геометрическую интуицию. См. `outputs/prompt-matrix-operations.md`.

Созданный здесь класс Matrix служит основой мини-фреймворка нейронной сети, который мы построим на этапе 3, в уроке 10.

## Упражнения

1. **Проверьте обратную матрицу.** Умножьте `A @ A.inverse_2x2()` и убедитесь, что получилась единичная матрица. Попробуйте с тремя различными матрицами 2x2. Что происходит, когда определитель равен нулю?

2. **Реализуйте обращение матрицы 3x3.** Расширьте класс Matrix, чтобы он вычислял обратные матрицы 3x3 методом присоединённой матрицы. Сравните результат с `np.linalg.inv` из NumPy.

3. **Создайте двухслойную сеть.** Используя только свой класс Matrix (без NumPy), создайте двухслойную нейронную сеть: вход (3) -> скрытый слой (4) -> выход (2). Инициализируйте случайные веса, выполните прямой проход и убедитесь, что все размерности верны.

## Ключевые термины

| Термин | Как обычно говорят | Что это означает на самом деле |
|------|----------------|----------------------|
| Вектор | «Стрелка» | Упорядоченный список чисел. В ИИ — точка в многомерном пространстве. |
| Матрица | «Таблица чисел» | Линейное преобразование. Оно отображает векторы из одного пространства в другое. |
| Умножение матриц | «Просто перемножить числа» | Скалярные произведения каждой строки первой матрицы с каждым столбцом второй. Порядок важен. |
| Транспонирование | «Перевернуть» | Поменять строки и столбцы местами. Превращает матрицу m x n в матрицу n x m. Критически важно при обратном распространении ошибки. |
| Определитель | «Какое-то число из матрицы» | Показывает, во сколько раз матрица масштабирует площадь (в 2D) или объём (в 3D). Нуль означает, что преобразование схлопывает одно измерение. |
| Обратная матрица | «Отменить матрицу» | Матрица, обращающая преобразование. Существует только тогда, когда определитель не равен нулю. |
| Единичная матрица | «Скучная матрица» | Матричный эквивалент умножения на 1. Используется в остаточных связях (ResNet). |
| Транслирование | «Магическое исправление размерностей» | Растягивание меньшего массива до размеров большего путём повторения по отсутствующим измерениям. |
| Поэлементная операция | «Обычное умножение» | Перемножение элементов в соответствующих позициях. Оба массива должны иметь одинаковую размерность (либо допускать транслирование). |

## Дополнительные материалы

- [3Blue1Brown: суть линейной алгебры](https://www.3blue1brown.com/topics/linear-algebra) — наглядное объяснение каждой рассмотренной здесь операции
- [Документация NumPy по транслированию](https://numpy.org/doc/stable/user/basics.broadcasting.html) — точные правила, которым следует NumPy
- [Обзор линейной алгебры Stanford CS229](http://cs229.stanford.edu/section/cs229-linalg.pdf) — краткий справочник по линейной алгебре для ML
