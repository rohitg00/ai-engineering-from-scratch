# 多模态智能体与计算机操作（毕业项目）

> 2026 年的前沿产品是一个多模态智能体：它能读取截图、点击按钮、操作网页 UI、填写表单，并端到端地完成整个工作流。SeeClick 和 CogAgent（2024）证明了 GUI grounding 这一基础能力。Ferret-UI 将其扩展到移动端。ChartAgent 引入了针对图表的视觉工具调用。VisualWebArena 和 AgentVista（2026）是前沿竞逐的基准——即使是 Gemini 3 Pro 和 Claude Opus 4.7 在 AgentVista 的困难任务上也只能达到约 30% 的得分。这个毕业项目把第 12 阶段的所有线索整合在一起：感知（高分辨率 VLM）、推理（带工具调用的 LLM）、grounding（坐标输出）、长程记忆，以及评估。

**类型：** 毕业项目
**语言：** Python（标准库，动作 schema + 智能体循环骨架）
**先修条件：** 第 12 阶段 · 05（LLaVA）、第 12 阶段 · 09（Qwen-VL JSON）、第 14 阶段（智能体工程）
**时长：** 约 240 分钟

## 学习目标

- 设计一个多模态智能体循环：感知 → 推理 → 行动 → 观察 → 重复。
- 构建一个 GUI grounding 输出 schema（点击坐标、输入文本、滚动、拖拽），供 VLM 以 JSON 形式输出。
- 对比纯截图智能体、无障碍树智能体与混合智能体。
- 在 VisualWebArena 的一小片数据上搭建多模态智能体基准评估。

## 问题

一个订票网站的工作流：“帮我找一张 4 月 15 日飞往东京的航班，靠过道座位，价格低于 800 美元，然后订票。”

一个多模态智能体需要：

1. 对浏览器截图。
2. 将截图 + URL + 目标解析成一个计划。
3. 输出一个结构化动作：点击（在 x,y 处）、输入 “Tokyo”（在元素 E 处）、向下滚动、选择（单选按钮）。
4. 将动作应用到浏览器。
5. 观察新状态（下一张截图）。
6. 重复，直到任务完成。

每一步都是一次多模态 VLM 调用。VLM 的输出必须是可解析的 JSON。错误会在多个步骤之间累积，因此恢复机制至关重要。

## 核心概念

### GUI grounding — 基础能力

GUI grounding 是指：给定一张截图和一条自然语言指令，输出要点击的 (x, y) 坐标（或其他动作）。

SeeClick（arXiv:2401.10935）是首个大规模的公开成果：在合成 + 真实 GUI 数据上微调 VLM，以纯文本 token 的形式输出坐标。有效。

CogAgent（arXiv:2312.08914）为密集 UI 增加了 1120x1120 的高分辨率编码。得分：网页导航约 84%。

Ferret-UI（arXiv:2404.05719）聚焦移动端 UI，并与 iOS 无障碍数据集成。

输出格式通常是 JSON：

```json
{"action": "click", "x": 384, "y": 220, "element_desc": "Search button"}
```

`element_desc` 有助于恢复：如果坐标在多张截图之间发生漂移，语义提示能让系统重新 grounding。

### 动作 schema

典型的动作 schema 包含 6-10 种动作类型：

- `click`：(x, y)
- `type`：(text, x?, y?)
- `scroll`：(direction, amount)
- `drag`：(x0, y0, x1, y1)
- `select`：(option_index)
- `hover`：(x, y)
- `navigate`：(url)
- `wait`：(ms)
- `done`：(success, explanation)

智能体每步输出一个动作。浏览器封装器执行动作并返回新状态。

### 纯截图 vs 无障碍树

两种输入模式：

- 纯截图：完整图像，无结构信息。最通用；适用于任何应用。
- 无障碍树：结构化的 DOM / iOS 无障碍信息。grounding 可靠得多；仅在树可用时有效。
- 混合：两者结合，用树作为原子动作的可靠 grounding 手段，用截图提供语义上下文。

生产级智能体在可能时使用混合模式。浏览器自动化（Selenium + 无障碍信息）总是有树可用；桌面应用则有时有。

### 长程记忆

一个 20 步的工作流会产生 20 张截图。VLM 的上下文很快就会被填满。三种压缩策略：

- 摘要链：每 5 步之后，总结已发生的事情，丢弃旧截图。
- 跳帧：保留第一张、最后一张，以及每隔 3 张的截图。
- 工具记录日志：执行动作时保留所做操作的文本日志；不再回看旧截图。

Claude 的 computer-use API 采用日志模式。更简单、更可靠。

### 视觉工具调用

ChartAgent（arXiv:2510.04514）为图表理解引入了视觉工具调用：裁剪、缩放、OCR、调用外部检测。智能体可以输出“裁剪到区域 (100, 200, 300, 400) 然后调用 OCR”作为一个工具调用。工具返回文本；VLM 继续推理。

这个模式可以泛化：set-of-mark 提示、区域标注和外部检测工具都符合同一套“输出工具调用，接收结构化响应”的 schema。

### 2026 年的基准

- ScreenSpot-Pro。约 1k 张网页截图上的 GUI grounding。开源 SOTA 为 Qwen2.5-VL-72B，约 85%。前沿模型约 90%。
- VisualWebArena。端到端网页任务（购物、论坛、分类信息）。开源 SOTA 约 20%。Gemini 3 Pro 约 27%。
- AgentVista（arXiv:2602.23166）。2026 年最难的基准。横跨 12 个领域的真实工作流。前沿模型得分 27-40%；开源模型 10-20%。
- WebArena / WebShop。较老的基准；已被前沿模型饱和。

### 为什么它依然困难

智能体性能的瓶颈：

1. 细粒度视觉 grounding。“点击那个小 X”在移动端分辨率下经常失败。
2. 长程规划。执行 10 个动作之后，智能体会偏离目标。
3. 错误恢复。当点击失败（点错按钮）时，检测并恢复这类场景在训练数据中很少见。
4. 跨页面上下文。在标签页或长表单之间跳转会丢失状态。

研究方向：记忆架构、显式重规划、多模态验证（用截图匹配验证动作是否成功）。

### 毕业项目的构建任务

毕业项目任务：构建一个 computer-use 智能体，它能够：

1. 读取一个订票网站模拟页面的 HTML + 截图。
2. 规划多步序列：搜索 → 选择 → 填表 → 提交。
3. 输出符合动作 schema 的 JSON 动作。
4. 在固定的 10 个任务切片上进行评估。

本课提供的脚手架代码可以轻松扩展为对接真实浏览器。

```figure
mm-agent-loop
```

## 使用它

`code/main.py` 是毕业项目的脚手架：

- 动作 schema 的 JSON 定义（10 个动作）。
- 以字典表示的模拟浏览器状态。
- 智能体循环骨架：接收状态、输出动作、应用动作、循环。
- 10 个任务的迷你基准（合成页面），用于衡量端到端成功率。
- 动作失败时的错误恢复钩子。

## 交付它

本课产出 `outputs/skill-multimodal-agent-designer.md`。给定一个 computer-use 产品（领域、动作集合、评估目标），设计完整的智能体循环、记忆策略、grounding 模式，以及预期的基准得分。

## 练习

1. 在动作 schema 中扩展一个 `screenshot_region` 工具（裁剪 + 缩放）。哪些任务会因此受益？

2. 阅读 AgentVista（arXiv:2602.23166）。描述最难的任务类别，并解释为什么前沿模型仍然失败。

3. 长程记忆压缩：设计一个摘要链，最多保留 4 张截图在线，其余数量不限均可记入日志。

4. 构建一个错误恢复钩子：动作失败（找不到按钮）时，智能体下一步该做什么？

5. 在 10 个网页任务上，对比纯截图的 Claude 4.7 与混合（截图 + 无障碍树）的 Qwen2.5-VL。哪个在哪些任务上胜出？

## 关键术语

| 术语 | 人们的说法 | 实际含义 |
|------|-----------------|------------------------|
| GUI grounding | “点击坐标” | 模型针对截图中指令的目标输出 (x,y) |
| 动作 schema | “工具定义” | 合法动作（click、type、scroll、drag）的 JSON 描述 |
| 无障碍树 | “结构化 DOM” | 来自浏览器/iOS API 的机器可读 UI 层级 |
| 混合智能体 | “截图 + 树” | 同时使用图像和结构化信息；比单独任何一种更可靠 |
| 视觉工具调用 | “缩放/裁剪/检测” | 智能体在规划中途调用外部视觉工具（OCR、检测） |
| 摘要链 | “记忆压缩” | 用周期性的文本摘要替代冗长的截图历史 |
| VisualWebArena | “端到端网页基准” | 2024 年面向端到端网页任务的基准 |
| AgentVista | “2026 高难度基准” | 12 个领域的真实工作流；连 Gemini 3 Pro 也只有约 30% |

## 延伸阅读

- [Cheng et al. — SeeClick (arXiv:2401.10935)](https://arxiv.org/abs/2401.10935)
- [Hong et al. — CogAgent (arXiv:2312.08914)](https://arxiv.org/abs/2312.08914)
- [You et al. — Ferret-UI (arXiv:2404.05719)](https://arxiv.org/abs/2404.05719)
- [ChartAgent (arXiv:2510.04514)](https://arxiv.org/abs/2510.04514)
- [Koh et al. — VisualWebArena (arXiv:2401.13649)](https://arxiv.org/abs/2401.13649)
- [AgentVista (arXiv:2602.23166)](https://arxiv.org/abs/2602.23166)