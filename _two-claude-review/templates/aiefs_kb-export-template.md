# {Lesson Title}

> Corpus KB entry. Generated by Reviewer after `APPROVED` verdict. Upload to the Corpus Knowledge Base Claude project as Project Knowledge.

## Identity

- **Source:** AI Engineering from Scratch — Phase `XX-name` / Lesson `NN-name`
- **Language:** Python | TS | Rust | Julia
- **Tags:** `#phase-XX` `#topic-1` `#topic-2`
- **Prerequisites:** Lesson references (e.g., `01-03 chain-rule-and-autodiff`)
- **Builds toward:** Later lesson references

## Core concept (the one thing to remember)

A single distilled paragraph. If you read only this, you should know what the lesson is about and why it matters.

## Mental model / analogy

The intuition pump. Often a metaphor or visual schema. Helps the concept stick.

## Canonical code (minimum viable example)

The smallest snippet that demonstrates the idea. Not the full lesson code — just the essence.

```python
# Example
def forward(x, w, b):
    return x @ w + b
```

## Key equations / invariants

- `y = f(x; θ)` — what each symbol means in this context
- Invariants: properties that must hold (e.g., "gradient sums to zero at optimum")

## Common pitfalls

- Mistake 1 → why it's wrong → correct approach
- Mistake 2 → ...

## Connections

- **Depends on:** `Lesson X-Y` (what concept is reused)
- **Used by:** `Lesson A-B` (how this becomes a building block)
- **Contrast with:** `Lesson P-Q` (related but distinct)

## Artifact shipped

If this lesson produced a reusable output, reference it:
- Prompt template: `outputs/prompts/foo.md`
- Skill: `outputs/skills/foo/SKILL.md`
- Agent: `outputs/agents/foo.ts`
- MCP server: `outputs/mcp-servers/foo/`

## One-question self-test

A single question that proves understanding. Answer not included — that's the point.
