{
  "lesson": "14-multi-round-trip-requests-and-elicitation",
  "title": "Multi Round-Trip Requests and Elicitation",
  "questions": [
    {
      "stage": "pre",
      "question": "A tool call needs the user to confirm a destructive action before a stateless 2026-07-28 server can finish it. What happens?",
      "options": [
        "The server opens a stream and pushes a server-initiated request while the original call stays open",
        "The server ends the call with an input_required result and the client retries as a new request",
        "The server blocks in place until a background worker resolves the confirmation for it",
        "The server stores the call in memory and waits for a separate confirmation endpoint to eventually be hit"
      ],
      "correct": 1,
      "explanation": "MRTR replaces server-initiated requests: the server ends the original call with resultType input_required, and the client gathers input, then retries the request independently."
    },
    {
      "stage": "check",
      "question": "When the client retries a tools/call after collecting the answers listed in inputRequests, what must be true of the JSON-RPC id?",
      "options": [
        "It reuses the id from the input_required response so the server can match up its state",
        "It is left out entirely because the retry is answered on the original response stream",
        "It is a brand new id, because the retry counts as an independent request entirely",
        "It is chosen by the server and returned to the client inside requestState itself"
      ],
      "correct": 2,
      "explanation": "The retry is a completely independent request, so its JSON-RPC id must differ from the id used on the original call that received input_required."
    },
    {
      "stage": "check",
      "question": "What is a client required to do with the requestState field carried on an input_required result?",
      "options": [
        "Decode it locally to check whether the encoded expiry timestamp has already passed",
        "Merge it into clientInfo so the server can read it back from a trusted field",
        "Cache it alongside the tool definition so future calls can reuse the same value",
        "Echo it back on the retry exactly, without inspecting or altering its contents"
      ],
      "correct": 3,
      "explanation": "requestState is opaque to the client; the client must echo it back unchanged on the retry and must not parse, inspect, or modify it in any way."
    },
    {
      "stage": "check",
      "question": "Which set of client requests may a server answer with an input_required result under MRTR?",
      "options": [
        "tools/call, prompts/get, and resources/read, and nothing else at all in the protocol",
        "Any client request, since input_required works as a general purpose pause signal here",
        "Only server/discover, because it is treated as the sole protocol entry point for it",
        "Only requests that already carry a progressToken value inside their _meta block"
      ],
      "correct": 0,
      "explanation": "Servers may send InputRequiredResult only on tools/call, prompts/get, and resources/read; every other client request must always finish as resultType complete."
    },
    {
      "stage": "post",
      "question": "A retry's requestState carries a valid signature and has not expired, but it was minted for a different authenticated principal than the one presenting it now. What should the server do?",
      "options": [
        "Accept it, since an unbroken HMAC signature already proves the state is trustworthy",
        "Accept it once, then flag the mismatch for a human to review during a later audit",
        "Reject it, because requestState stays bound to the one principal it was issued to",
        "Accept it only after asking the model to confirm the principal in plain text first"
      ],
      "correct": 2,
      "explanation": "Integrity alone is not enough: requestState must be bound to the authenticated principal it was minted for, and a mismatch must be rejected even when the signature verifies."
    },
    {
      "stage": "post",
      "question": "A deploy tool needs the user's payment card number before it can continue. How should it collect that value under 2026-07-28?",
      "options": [
        "Send a form mode elicitation/create request with a requestedSchema for the card fields",
        "Use URL mode elicitation so the interaction stays outside the MCP client itself",
        "Place the request inside clientInfo so the client can prefill it for the model",
        "Skip elicitation and let the model infer a plausible value from the conversation"
      ],
      "correct": 1,
      "explanation": "Servers must not use form mode for sensitive data such as payment credentials; URL mode elicitation keeps the interaction out of band so the value never transits the MCP client."
    }
  ]
}
