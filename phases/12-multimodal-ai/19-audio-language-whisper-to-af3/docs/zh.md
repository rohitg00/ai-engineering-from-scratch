# 音频-语言模型：从 Whisper 到 Audio Flamingo 3 的演进之路

> Whisper（Radford 等人，2022 年 12 月）确立了语音识别领域的标准——基于 68 万小时弱监督多语言语音数据、一个简洁的编码器-解码器 Transformer，以及一个让后续所有 ASR 发布都不得不引用的基准测试。但识别不等于推理。问出"这段录音里有什么乐器"、"说话者表达的是什么情绪"或"第三分钟发生了什么"这类问题，需要的是音频理解能力，而非单纯的转录。Qwen-Audio、SALMONN、LTU 和 NVIDIA 的 Audio Flamingo 3（AF3，2025 年 7 月）逐步构建了这一技术栈：保留 Whisper 类的编码器，接入 Q-Former，在音频-文本指令数据上训练，并加入思维链推理能力。本课程将带你走完这条演进之路。

**类型：** 构建
**语言：** Python（标准库，log-Mel 语谱图 + 音频 Q-Former 骨架）
**前置知识：** 阶段 6（语音与音频），阶段 12 · 03（Q-Former）
**时长：** 约 180 分钟

## 学习目标

- 从波形计算 log-Mel 语谱图：加窗、FFT、滤波器组、对数变换。
- 比较编码器选项：Whisper 编码器、BEATs、AF-Whisper 混合方案。各自的适用场景。
- 构建音频 Q-Former：N 个可学习查询向量与语谱图像块进行交叉注意力计算。
- 解释级联（Whisper 接 LLM）与端到端音频-LLM 训练的区别：为什么端到端在推理方面更具可扩展性。

## 问题

Whisper 解决了语音识别问题。音频版的 OCR 已成为一项基础能力。但"基础能力"止步于转录。如果模型无法对其听到的内容进行推理——时间、说话人、情绪、音乐结构、环境声音——那么单纯的转录无法驱动产品功能。

三种显而易见的路径：

1. **级联：** Whisper 转录，LLM 对文本进行推理。适用于纯语音场景。对音乐、环境音频、多人重叠说话、情绪推理无效。
2. **端到端音频-LLM：** 音频编码器直接将音频令牌送入 LLM，跳过转录环节。保留声学信息（情绪、说话人、环境）。需要新的训练数据。
3. **混合方案：** 音频编码器 + 既能转录又能推理的文本解码器。Qwen-Audio 和 Audio Flamingo 选择了这条路径。

## 核心概念

### Log-Mel 语谱图：输入特征

每个音频编码器都从同一个特征开始：log-Mel 语谱图。

1. 重采样至 16 kHz。
2. 短时傅里叶变换，窗口 25ms，步长 10ms。
3. 取 FFT 结果的幅度。
4. 应用 Mel 滤波器组（通常为 80 个滤波器，在 0-8000 Hz 范围内对数间隔），映射到感知频率尺度。
5. 对数压缩（log(1 + x)）以调整动态范围。

结果：一个形状为 (T, 80) 的二维数组，其中 T 是时间帧数。对于 30 秒的片段，帧率 100 Hz：结果为 (3000, 80)。

### Whisper 的编码器

Whisper 的编码器是一个 12 层的 ViT 风格 Transformer，将 log-Mel 语谱图作为时间帧序列处理。输出：每个时间帧一个隐藏状态向量。

对于 ASR，Whisper 的解码器是一个交叉注意力 Transformer，根据编码器输出生成文本令牌。标准的编码器-解码器架构。

对于 ALM（音频-LLM），你希望将编码器输出作为另一个不同 LLM 的输入。典型模式：Whisper 编码器冻结，Q-Former 可训练，LLM 冻结或微调。

### BEATs 与专用音频编码器

Whisper 在语音主导的数据上训练。对音乐和环境音频的表现较弱。

BEATs（Chen 等人，2022）是一个在 AudioSet 上自监督训练的 Transformer。在相同参数量下，对音乐和环境声音的捕捉能力优于 Whisper。

AF-Whisper（Audio Flamingo 3 的混合方案）：将 Whisper + BEATs 的特征拼接作为音频输入。Whisper 携带语言信号，BEATs 携带声学信号。

### 音频 Q-Former

与 BLIP-2 的视觉 Q-Former 模式相同。固定数量的可学习查询向量（通常为 32 或 64）对音频编码器的输出帧进行交叉注意力计算。这些查询向量成为供 LLM 使用的音频令牌。

对齐阶段训练：仅训练 Q-Former，在音频-文本对（AudioCaps、Clotho）上使用对比 + 描述损失。指令阶段：端到端训练，解冻 LLM，在指令数据上训练。

### 演进之路——SALMONN、Qwen-Audio、AF3

**SALMONN**（Tang 等人，2023）：Whisper + BEATs + Q-Former + LLaMA。首个具备较强推理能力的开放音频-LLM。在 MMAU 基准上综合得分约 0.55。

**Qwen-Audio**（Chu 等人，2023）：架构类似，在更丰富的数据集上训练，针对多轮对话进行了优化。MMAU 约 0.60。

**LTU——Listen, Think, Understand**（Gong 等人，2023）：显式的推理数据，专注于音频片段上的思维链。规模更小但更加专注。

**Audio Flamingo 3**（Goel 等人，2025 年 7 月）：当前开放 SOTA。8B LLM 骨干（Qwen2 7B），Whisper-large 编码器拼接 BEATs，64 查询 Q-Former，在 100 万+ 音频-文本指令对上训练。MMAU 0.72，在某些子任务上与闭源前沿模型持平。

AF3 还引入了音频领域的按需思维链：模型可以在给出最终答案前选择性地生成思考令牌（"让我先识别乐器：……"）。启用思考后，复杂推理任务的准确率提升 3-5 个百分点。

### 级联 vs 端到端

**级联流水线：**

1. Whisper 将音频转录为文本。
2. LLM 对文本进行推理。

对于"总结这个播客"这类任务效果完美。但在以下场景中失败：
- "这首歌的情绪是什么？"——情绪存在于声音中，而非文字里。
- "说话的是 Alice 还是 Bob？"——需要说话人识别。
- "爆炸发生在第几秒？"——时间定位在文本中丢失。
- "这是真实音频还是生成的？"——深度伪造检测需要声学特征。

**端到端**方案保留了声学信号。Qwen-Audio 和 AF3 天然支持音乐、环境和情绪理解。

### 2026 年生产环境方案

对于一个全新的音频理解产品：

- **选择级联方案：** 目标是转录，不涉及音乐、情绪推理。
- **选择 AF3 / Qwen-Audio 系列：** 涉及音乐、情绪、多人说话或复杂音频推理。

级联更便宜、更简单。端到端能力更强。

### MMAU——音频推理基准

MMAU（Massive Multimodal Audio Understanding，大规模多模态音频理解）是 2024-2025 年的音频推理基准：

- 10,000 个涵盖语音、音乐、环境声音的音频-文本问答对。
- 涵盖分类、时序推理、因果推理、开放式问答。
- 测试级联流水线系统性遗漏的能力。

开放 SOTA（AF3）得分 0.72；闭源前沿约 0.78（Gemini 2.5 Pro、Claude Opus 4.7）。差距小于 VideoMME 的开放 vs 闭源差异，表明音频-LLM 正在走向成熟。

## 动手实践

`code/main.py`：

- 使用标准库实现 log-Mel 语谱图计算：加窗、朴素 DFT、Mel 滤波器组。
- 音频 Q-Former 骨架：给定编码器输出帧，计算 Q、K、V、注意力，并输出 N 个令牌。
- 在玩具任务上比较级联 vs 端到端方案。

## 交付物

本课程生成 `outputs/skill-audio-llm-pipeline-picker.md`。给定一个音频任务（转录、音乐标注、情绪推理、多人说话人分离、环境分类），它将选择级联方案、端到端 AF3 方案或混合方案。

## 练习

1. 计算 30 秒片段在 16kHz、25ms 窗口、10ms 步长、80 个 Mel 滤波器下的 log-Mel 语谱图维度。在 48kHz 下如何变化？

2. 为什么 Whisper 在音乐上表现不佳？BEATs 捕捉了哪些 Whisper 未能捕捉的音频特征？

3. 音频 Q-Former 使用 64 个查询 vs 32 个查询：在什么任务复杂度下 64 个查询更值得？32 个查询在哪些场景下能节省计算资源？

4. 阅读 AF3 论文第 4 节关于按需思考的内容。提出三个思维链最有帮助的音频任务。

5. 使用 AF3 的输出实现一个最小化的说话人分离流水线。如何标识说话人切换？

## 关键术语

| 术语 | 通常说法 | 实际含义 |
|------|---------|---------|
| Log-Mel 语谱图 | "Mel 特征" | 经过 Mel 滤波器组后的二维（时间，频率）对数幅度数组 |
| 音频 Q-Former | "音频感知器" | 从音频编码器输出到固定长度查询向量的交叉注意力瓶颈，供 LLM 使用 |
| 级联 | "ASR 接 LLM" | Whisper 转录 + 文本 LLM 推理的流水线；丢失声学信息 |
| 端到端 | "音频-LLM" | 通过 Q-Former 将音频特征直接送入 LLM；保留声学信号 |
| BEATs | "音频 AudioSet 编码器" | 在 AudioSet 上自监督训练的 Transformer；在音乐和环境声音上表现优异 |
| MMAU | "音频推理基准" | 涵盖语音、音乐、环境的 1 万问答对；2024 年评估标准 |
| 按需思考 | "音频思维链" | 模型可选择在最终答案前生成推理令牌，准确率提升 3-5 个百分点 |

## 延伸阅读

- [Radoff 等人 — Whisper (arXiv:2212.04356)](https://arxiv.org/abs/2212.04356)
- [Chu 等人 — Qwen-Audio (arXiv:2311.07919)](https://arxiv.org/abs/2311.07919)
- [Goel 等人 — Audio Flamingo 3 (arXiv:2507.08128)](https://arxiv.org/abs/2507.08128)
- [Tang 等人 — SALMONN (arXiv:2310.13289)](https://arxiv.org/abs/2310.13289)
- [Gong 等人 — LTU (arXiv:2305.10790)](https://arxiv.org/abs/2305.10790)
