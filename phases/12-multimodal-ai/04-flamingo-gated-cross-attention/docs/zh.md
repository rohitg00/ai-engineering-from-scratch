# Flamingo 与门控交叉注意力：少样本视觉语言模型

> DeepMind 的 Flamingo（2022）率先完成了两件事。它证明了一个单一模型可以处理任意交错的图像、视频和文本序列。它还证明了视觉语言模型可以上下文学习（in-context learning）——给出一个包含三个示例（图像、描述）对的少样本提示，模型就能描述新图像，无需任何梯度步骤。其机制是：在冻结的大语言模型现有层之间插入门控交叉注意力层，并带有一个从零开始学习的 tanh 门控，使得大语言模型的文本能力在初始化时得以保留。本课程讲解 Flamingo 的 Perceiver 重采样器和门控交叉注意力架构——这是 Gemini 交错输入和 Idefics2 视觉令牌的祖先。

**类型：** 学习
**语言：** Python（标准库，门控交叉注意力 + Perceiver 重采样器演示）
**前置知识：** 阶段 12 · 03（BLIP-2 Q-Former）
**时长：** 约 120 分钟

## 学习目标

- 解释门控交叉注意力如何通过 tanh(门控) = 0 在初始化时保留冻结大语言模型的文本能力。
- 讲解 Perceiver 重采样器：通过交叉注意力将 N 个图像块转换为 K 个固定的"潜在"查询。
- 描述 Flamingo 如何处理交错的图像-文本序列，并使用尊重图像位置的因果掩码。
- 重现一个少样本多模态提示结构（3 个图像-描述示例，然后是一个查询图像）。

## 问题

BLIP-2 将 32 个视觉令牌输入到冻结的大语言模型输入层。每个提示一张图像没问题。但如果要在提示中交错*多张*图像和文本呢？例如"这是图像 A，描述它；这是图像 B，描述它；现在这是图像 C，描述它"？大语言模型的自注意力需要在一个流中处理图像令牌和文本令牌，而哪些位置可以关注哪些图像的问题也变得棘手。

Flamingo 的答案：完全不改变大语言模型的输入流。在现有的 LLM 块之间插入额外的交叉注意力层。文本令牌仍然像往常一样流经大语言模型的因果自注意力。在每几个 LLM 块之间，文本令牌还通过一个新的门控层交叉关注图像特征。该门控（初始化为零）意味着在零步时新层是不起作用的——模型的行为与预训练的大语言模型完全相同。随着训练的进行，门控打开，视觉信息开始流入。

Flamingo 回答的第二个问题：如何处理每个提示中数量不定的图像（0 张、1 张或多张）？答案是 Perceiver 重采样器——一个小的交叉注意力模块，它接收任意数量的图像块并产生固定数量的视觉潜在令牌。无论提示中有多少张图像，大语言模型的交叉注意力层看到的形状都是相同的。

## 概念

### 冻结的大语言模型

Flamingo 从一个冻结的 Chinchilla 70B 大语言模型开始。所有 700 亿权重都未改动。现有的文本自注意力和 FFN 正常运作。

### Perceiver 重采样器

对于提示中的每张图像，ViT 产生 N 个图像块令牌。Perceiver 重采样器有 K 个固定的可学习潜在向量（Flamingo 使用 K=64）。每个重采样器块包含两个子步骤：

1. 交叉注意力：K 个潜在向量关注 N 个图像块令牌（查询来自潜在向量，键/值来自图像块）。
2. 潜在向量内部的自我注意力 + FFN。

经过 6 个重采样器块后，输出是 K=64 个维度为 1024 的视觉令牌，无论 ViT 产生了多少个图像块。一张 224x224 的图像（196 个图像块）和一张 480x480 的图像（900 个图像块）都输出为 64 个重采样器令牌。

对于视频，重采样器在时间维度上应用：每帧的图像块产生 64 个潜在向量，时间位置编码让模型能够区分 t=0 和 t=N。整个视频变成 T * 64 个视觉令牌。

### 门控交叉注意力

在冻结大语言模型的每 M 层之间（Flamingo 使用 M=4），插入一个新的门控交叉注意力块：

```
x_after_llm_block = llm_block(x_before)
cross = cross_attn(x_after, resampler_output)
gated = tanh(alpha) * cross + x_after
x_before_next_block = gated
```

- `alpha` 是一个可学习的标量，初始化为零。
- `tanh(0) = 0`，因此在初始化时门控分支的贡献为零。
- 随着 `alpha` 偏离零，交叉注意力的贡献平滑增长。
- 残差连接意味着即使门控完全打开，也不会覆盖大语言模型的文本表示；它只是在之上叠加了视觉信息。

这是 Flamingo 中最重要的一个设计选择：视觉条件化是加性的、带门控的，并且在初始化时为零。第 0 步的 Flamingo 在纯文本输入上就是一个完美的 Chinchilla 70B。

### 用于交错输入的掩码交叉注意力

在像"<图像 A> 描述 A <图像 B> 描述 B <图像 C> ?"这样的提示中，每个文本令牌应该只看到序列中位于它之前的图像。交叉注意力掩码强制执行：位置 `t` 处的文本令牌只关注图像索引 `i < i_t` 的图像重采样器令牌，其中 `i_t` 是位置 `t` 之前最近的图像索引。"只看到前一张图像"和"看到所有前面的图像"都是有效的选择；Flamingo 选择了前者。

### 上下文少样本学习

一个 Flamingo 提示看起来像：

```
<图像1> 一张猫的照片。<图像2> 一张狗的照片。<图像3> 一张
```

模型看到补全模式并输出"鸟"（或图像3展示的任何内容）。没有梯度步骤。冻结的大语言模型的上下文学习能力通过门控交叉注意力得以保留——这是论文的核心亮点，也是它如此重要的原因。

### 训练数据

Flamingo 在三个数据集上训练：

1. MultiModal MassiveWeb (M3W)：4300 万张包含交错图像和文本的网页，重建了阅读顺序。
2. 图像-文本对（ALIGN + LTIP）：44 亿对。
3. 视频-文本对 (VTP)：2700 万个短视频片段。

OBELICS（2023）是交错网页语料库的开放复现，Idefics、Idefics2 和大多数开放的"类 Flamingo"模型都在其上训练。

### OpenFlamingo 和 Otter

OpenFlamingo（2023）是开放的复现版本。架构相同（Perceiver 重采样器 + 冻结 LLaMA 或 MPT 上的门控交叉注意力）。提供 3B、4B、9B 检查点。由于基础大语言模型较小且数据较少，质量落后于 Flamingo。

Otter（2023）在 OpenFlamingo 基础上使用 MIMIC-IT（一个多模态指令数据集）进行指令微调，展示了门控交叉注意力也适用于指令跟随。

### 后续发展

- Idefics / Idefics2 / Idefics3：Hugging Face 的门控交叉注意力系列，逐步简化（Idefics2 放弃了重采样器，转而使用带自适应池化的直接图像块令牌）。
- Flamingo 到 Chameleon 的转变：到 2024 年，许多团队转向了早期融合（第 12.11 课）；Flamingo 风格的门控交叉注意力在需要冻结骨干网络的场景中仍被用于生产环境。
- Gemini 的交错输入：概念上继承了 Flamingo 交错格式的灵活性，但具体机制是专有的。

### 与 BLIP-2 的对比

| | BLIP-2 | Flamingo |
|---|---|---|
| 视觉桥接 | 输入处一次 Q-Former | 每 M 层进行一次门控交叉注意力 |
| 视觉令牌 | 每张图像 32 个 | 每张图像每个交叉注意力层 64 个 |
| 冻结大语言模型 | 是 | 是 |
| 少样本上下文学习 | 弱 | 强——论文的核心亮点 |
| 交错输入 | 无原生支持 | 是，设计目标 |
| 训练数据 | 1.3 亿对 | 13 亿对 + 4300 万交错网页 |
| 参数量 | 1.88 亿可训练 | 约 100 亿可训练（交叉注意力层） |
| 计算资源 | 8 张 A100 上数天 | 数千张 TPUv4 上数周 |

预算有限、单张图像的 VQA 选择 BLIP-2。需要交错、少样本或多图像推理时选择 Flamingo/Idefics2。

## 使用

`code/main.py` 演示了：

1. 一个 Perceiver 重采样器，处理 36 个假图像块令牌，使用 8 个可学习潜在向量（纯 Python 交叉注意力）。
2. 一个门控交叉注意力步骤，`alpha = 0` 时输出等于输入（LLM 不变），然后 `alpha = 2.0` 时视觉贡献混合进来。
3. 一个交错掩码构建器，为"（图像 1）（文本 1）（图像 2）（文本 2）"序列生成二维注意力掩码。

## 交付物

本课程生成 `outputs/skill-gated-bridge-diagnostic.md`。给定一个开放视觉语言模型的配置（是否有重采样器、交叉注意力频率、门控方案），识别 Flamingo 系列元素并解释冻结策略。有助于调试微调后文本性能下降的问题（答案：门控打开得太快太宽）。

## 练习

1. 计算 Flamingo-9B 的视觉参数量：9B LLM + 1.4B 门控交叉注意力层 + 64M 重采样器。可训练参数占总参数的比例是多少？

2. 在 PyTorch 中实现门控残差 `y = tanh(alpha) * cross + x`。通过实验证明 `alpha=0` 时 `y==x` 在初始化时精确成立。

3. 阅读 OpenFlamingo 第 3.2 节（arXiv:2308.01390），了解当每个提示具有不同图像数量时，它们如何处理批次中的多张图像。描述填充策略。

4. 为什么 Flamingo 的交叉注意力掩码让文本令牌只关注*最近的前一张*图像，而不是所有前面的图像？阅读 Flamingo 论文第 2.4 节并解释其中的权衡。

5. 上下文少样本学习：为一个新的 Flamingo 变体构建一个包含 4 个"图像 → 主要物体颜色"示例的提示。描述当示例数量从 0 变化到 8 时的预期准确率模式。

## 关键术语

| 术语 | 人们怎么说 | 实际含义 |
|------|-----------|---------|
| Perceiver 重采样器 | "固定潜在向量的交叉注意力" | 从数量可变的输入图像块中产生 K 个固定令牌的模块 |
| 门控交叉注意力 | "tanh 门控桥接" | 残差层 `y = tanh(alpha)*cross + x`，alpha 可学习，初始化为 0 |
| 交错输入 | "混合序列" | 图像和文本按阅读顺序自由混合的提示格式 |
| 冻结大语言模型 | "无 LLM 梯度" | 文本大语言模型的权重不更新；只有重采样器 + 交叉注意力层训练 |
| 少样本 | "上下文示例" | 在提示中给出几个（图像，答案）对；模型无需微调即可泛化 |
| OBELICS | "交错网页语料库" | 包含 1.41 亿网页的开放数据集，图像和文本按阅读顺序排列 |
| Chinchilla | "700 亿冻结基础模型" | Flamingo 的冻结文本大语言模型，来自 DeepMind 的 Chinchilla 论文 |
| 门控调度 | "alpha 如何变化" | 训练过程中交叉注意力门控打开的速度 |
| 交叉注意力频率 | "每 M 层一次" | 门控交叉注意力块插入的频率；Flamingo 使用 M=4 |
| OpenFlamingo | "开放复现" | MosaicML/LAION 的 3-9B 开放检查点；架构与 Flamingo 相同 |

## 延伸阅读

- [Alayrac 等人 — Flamingo（arXiv:2204.14198）](https://arxiv.org/abs/2204.14198) —— 原始论文。
- [Awadalla 等人 — OpenFlamingo（arXiv:2308.01390）](https://arxiv.org/abs/2308.01390) —— 开放复现。
- [Laurençon 等人 — OBELICS（arXiv:2306.16527）](https://arxiv.org/abs/2306.16527) —— 交错网页语料库。
- [Jaegle 等人 — Perceiver IO（arXiv:2107.14795）](https://arxiv.org/abs/2107.14795) —— 通用 Perceiver 架构。
- [Li 等人 — Otter（arXiv:2305.03726）](https://arxiv.org/abs/2305.03726) —— 指令微调的 Flamingo 后续模型。
- [Laurençon 等人 — Idefics2（arXiv:2405.02246）](https://arxiv.org/abs/2405.02246) —— Flamingo 方法的现代简化版本。
