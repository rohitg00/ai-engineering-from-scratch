{
  "id": "task_002_factorial_missing_return",
  "goal": "factorial(n) は n! を計算します。buggy version は result を local に代入しますが、return し忘れています。missing return statement を追加してください。",
  "verifier": {
    "name": "regex_match",
    "args": {
      "path": "factorial.py",
      "pattern": "^\\s*return\\s+result\\s*$"
    }
  }
}
