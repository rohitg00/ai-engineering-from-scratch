---
name: quantization-picker
description: 根据硬件、引擎、工作负载和质量容忍度选择 2026 年量化格式，并制定校准和验证方案。
version: 1.0.0
phase: 17
lesson: 09
tags: [quantization, awq, gptq, gguf, fp8, nvfp4, calibration]
---

给定硬件（CPU / H100 / H200 / B200 / GB200，含数量）、引擎（llama.cpp / vLLM / TRT-LLM / SGLang）、模型（大小 + 任务类型——常规聊天/推理/代码/多 LoRA）和质量容忍度（能在 HumanEval / MATH / MMLU 上承受 N 分下降），选择量化格式并制定验证方案。

输出：

1. **格式推荐**。以下之一：GGUF Q4_K_M、GGUF Q5_K_M、GPTQ-Int4 + Marlin、AWQ-Int4 + Marlin、FP8、NVFP4 + FP8 KV，或堆叠组合。按决策树论证：CPU → GGUF；推理 → FP8；vLLM 上的多 LoRA → GPTQ；常规 GPU 聊天 → AWQ；已验证的 Blackwell → NVFP4。
2. **内存预算**。报告权重 + KV 缓存（在报告并发 × 上下文下）+ 激活值。确认能容纳在目标 GPU 上，或指出需要多 GPU。
3. **校准方案**。数据集来源（AWQ/GPTQ 使用领域匹配数据；通用 C4/WikiText 作为最后手段）。样本数量（领域数据 500-2000 条）。验证集（从校准池中预留 10%）。
4. **验证方案**。匹配任务的评估集：代码用 HumanEval、推理用 MATH/MMLU、聊天用 MT-Bench。基线的 BF16 与量化后对比。如果下降 ≤ 质量容忍度则上线。
5. **KV 缓存决策**。与权重量化分开。推理场景推荐 FP8 KV；注意力精度边缘时用 BF16 KV；INT8 KV 仅在验证后使用。
6. **回滚路径**。在磁盘上保留 BF16/FP8 权重；如果生产质量下降，标记可切回。

**硬性拒绝条件：**
- 在推理密集型工作负载上推荐 NVFP4 权重但未经过评估集验证。
- 对领域模型在通用网页数据上校准。始终使用领域内数据。
- 在 HBM 预算中忘记 KV 缓存。始终逐项列出。
- 声称吞吐量数据但未指明内核（Marlin-AWQ 与普通 AWQ 相差 10 倍）。

**拒绝规则：**
- 如果工作负载本质上是质量边缘型（开放式创意生成、边缘案例推理），拒绝激进的 INT4。保持 FP8 或 BF16。
- 如果引擎是 llama.cpp，拒绝 GGUF 以外的任何格式。格式与引擎匹配是入门级要求。
- 如果用户无法运行 1,000 样本评估，拒绝。生产环境中不允许盲目量化。

**输出**：一页量化选择，列出所选格式、HBM 预算、校准方案、验证方案、KV 缓存决策和回滚路径。最后根据关键风险，指出"下一步测量什么"段落，指明评估集差值、峰值并发下的 KV 缓存压力或实际批次大小下的吞吐量。
