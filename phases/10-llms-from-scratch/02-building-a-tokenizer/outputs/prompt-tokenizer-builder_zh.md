---
name: prompt-tokenizer-builder
description: 为 LLM 项目构建和调试生产级分词器
version: 1.0.0
phase: 10
lesson: 2
tags: [tokenizer, bpe, byte-level, special-tokens, chat-template, multilingual]
---

# 生产级分词器构建器

为 LLM 项目构建或调试分词器时，遵循此框架。

## 流水线检查清单

每个生产级分词器都需要以下五个阶段。如果缺少任何一个，你会在生产中遇到边界情况。

1. **规范化 (Normalize)** -- 应用 NFKC Unicode 规范化。这会折叠连字（"fi" -> "fi"）、规范化全角字符、标准化空白。跳过此步骤，同一个词会根据输入方式获得不同的 token ID。

2. **预分词 (Pre-Tokenize)** -- 在 BPE 之前将文本拆分为块。英语为主的模型使用 GPT-2 的正则表达式模式。多语言模型使用 SentencePiece 的原始字节方法。此选择决定 BPE 是否可以跨词边界合并。

3. **BPE 合并 (BPE Merge)** -- 将学习到的合并表应用于每个块内的字节序列。合并表就是分词器的学习知识。其他一切都是管道工程。

4. **特殊 Token 注入 (Special Token Injection)** -- 在 BPE 运行前精确匹配特殊 token。[BOS]、[EOS]、[PAD]、对话模板标记获得固定 ID。它们从不参与合并。

5. **ID 映射 (ID Mapping)** -- 将 token 字符串转换为整数。模型只看到整数。

## 调试分词器问题

**症状：模型在对话输入上产生乱码**
- 检查对话模板。每个模型有不同的格式。Llama 3 使用 `<|start_header_id|>` 标记。ChatGPT 使用 `<|im_start|>` 标记。错误的模板会将输入置于训练分布之外。

**症状：非英文文本使用太多 token**
- 检查 fertility（每词 token 数）。高于 2.0 意味着分词器在该语言上浪费上下文窗口。解决方案：用更多多语言数据重新训练、增加词表大小，或使用 Unigram 的 SentencePiece。

**症状：数字和算术失败**
- 检查数字如何被分词。"1234" 作为一个 token 意味着模型无法进行数字级操作。在预分词阶段将数字单独拆分。

**症状：代码 token 效率低下**
- 检查缩进如何处理。GPT-2 的分词器在空格上浪费 token。Codex 和 StarCoder 使用特殊缩进 token（4 个空格 = 1 个 token）。

## 词表大小决策

- 32K token：单语言、小模型、有限算力。嵌入层为 32K * d_model 参数。
- 50K-64K：多语言或代码密集型。大多数项目的良好平衡。
- 100K+ (GPT-4, Llama 3)：仅当你有海量训练数据时。序列更短，但嵌入参数为 100K * d_model。

对于 4096 维模型：32K 词表 = 131M 嵌入参数。128K 词表 = 524M 嵌入参数。仅嵌入层就有 400M 参数。

## 速度要求

- 训练数据分词：使用 Rust 后端库（tiktoken、HuggingFace tokenizers）。纯 Python 慢 10-100 倍。
- 推理分词：延迟影响较小（单序列），但仍应使用编译实现。
- 基准测试：分词 1GB 文本并测量实际耗时。如果超过 60 秒，切换到 Rust 后端。

## 对话模板验证

部署任何对话模型前，验证模板：

1. 用分词器编码已知对话
2. 将其解码回文本
3. 与模型文档中的预期格式逐字符比较
4. 注意：header token 后的换行、内容前的空格、轮次结束标记
5. 测试边界情况：空系统消息、超长用户消息、多轮助手回复

对话模板错误是对话模型性能下降的最常见原因。
