---
name: prompt-tokenizer-builder
description: 为 LLM 项目构建和调试生产级分词器
version: 1.0.0
phase: 10
lesson: 2
tags: [tokenizer, bpe, byte-level, special-tokens, chat-template, multilingual]
---

# 生产级分词器构建器

在为 LLM 项目构建或调试分词器时，请遵循此框架。

## 流水线检查清单

每个生产级分词器都需要以下五个阶段。缺少任何一个都会导致生产环境中的边界问题。

1. **标准化** -- 应用 NFKC Unicode 标准化。这会合并连字（"ﬁ" -> "fi"）、规范化全角字符、标准化空白。跳过这一步，同一个词可能因输入方式不同而得到不同的 token ID。

2. **预分词** -- 在 BPE 之前将文本拆分为块。对以英语为中心的模型使用 GPT-2 的正则表达式模式。对多语言模型使用 SentencePiece 的原生字节方法。该选择决定了 BPE 能否跨词边界进行合并。

3. **BPE 合并** -- 对每个块内的字节序列应用学习到的合并表。合并表就是分词器学到的知识。其他一切都只是管道。

4. **特殊 Token 注入** -- 在 BPE 运行之前精确匹配特殊 token。[BOS]、[EOS]、[PAD]、聊天模板标记获得固定 ID。它们从不参与合并。

5. **ID 映射** -- 将 token 字符串转换为整数。模型只看到整数。

## 调试分词器问题

**症状：模型在聊天输入上产生垃圾输出**
- 检查聊天模板。每个模型都有不同格式。Llama 3 使用 `<|start_header_id|>` 标记。ChatGPT 使用 `<|im_start|>` 标记。错误的模板会将输入置于训练分布之外。

**症状：非英语文本使用过多 token**
- 检查产出率（每词的 token 数）。超过 2.0 意味着分词器在该语言上浪费上下文窗口。解决方案：用更多多语言数据重新训练、增加词汇量大小、或使用带 Unigram 的 SentencePiece。

**症状：数字和算术失败**
- 检查数字如何被分词。"1234" 作为一个 token 意味着模型无法进行逐位运算。在预分词期间将数字单独拆分。

**症状：代码 token 效率低下**
- 检查缩进如何处理。GPT-2 的分词器在空格上浪费 token。Codex 和 StarCoder 使用特殊的缩进 token（4 个空格 = 1 个 token）。

## 词汇量大小决策

- 32K token：单语言、小模型、有限计算。嵌入层为 32K * d_model 参数。
- 50K-64K：多语言或代码密集型。对大多数项目来说是不错的平衡。
- 100K+（GPT-4、Llama 3）：仅当拥有海量训练数据时。序列更短，但有 100K * d_model 嵌入参数。

对于 4096 维模型：32K 词汇量 = 1.31 亿嵌入参数。128K 词汇量 = 5.24 亿嵌入参数。仅嵌入层就多出 4 亿参数。

## 速度要求

- 训练数据分词：使用 Rust 后端的库（tiktoken、HuggingFace tokenizers）。纯 Python 慢 10-100 倍。
- 推理分词：延迟不太重要（单条序列），但仍应使用编译实现。
- 基准测试：对 1GB 文本进行分词并测量墙钟时间。如果超过 60 秒，切换到 Rust 后端。

## 聊天模板验证

在部署任何聊天模型之前，验证模板：

1. 使用分词器编码一个已知对话
2. 将其解码回文本
3. 逐字符与模型文档中的预期格式进行比较
4. 注意：头部 token 后的换行、内容前的空格、轮次结束标记
5. 测试边界情况：空系统消息、超长用户消息、多轮助手回复

聊天模板出错是导致聊天模型性能下降的最常见原因。
