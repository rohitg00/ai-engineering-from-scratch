---
name: prompt-sft-data-curator
description: 为监督微调设计和策划指令数据集
version: 1.0.0
phase: 10
lesson: 6
tags: [sft, instruction-tuning, fine-tuning, data-curation, alignment]
---

# SFT 数据策划器

在为特定能力（代码生成、数学、对话、安全）设计指令微调数据集时，使用此框架规划数据收集、定义质量标准并构建训练流水线。

## 输入要求

提供：
- **目标能力**（例如"Python 代码生成"、"医疗问答"、"多轮对话"）
- **基础模型**（例如 Llama 3 8B、Mistral 7B、Qwen 2.5 72B）
- **预算**（标注工时、合成生成的 API 成本）
- **格式偏好**（Alpaca、ShareGPT、ChatML）

## 第 1 步：数据集设计

### 规模指南

| 质量等级 | 所需示例数 | 预期效果 |
|--------------|----------------|------------------|
| 研究原型 | 1,000-5,000 | LIMA 质量：如果示例由专家编写，可与更大数据集媲美 |
| 生产 v1 | 10,000-50,000 | Stanford Alpaca 水平：常见任务上扎实的指令遵循 |
| 生产 v2 | 50,000-200,000 | Vicuna/Llama 2 Chat 水平：稳健的多轮对话、领域覆盖 |

质量始终胜过数量。1,000 个专家编写的示例（LIMA，2023 年 5 月）与在 50,000+ 示例上训练的模型相匹配。优先级：

1. **多样性**——覆盖目标能力的全部范围
2. **准确性**——每个响应必须事实正确
3. **清晰度**——响应应简洁且结构良好
4. **难度梯度**——包含简单、中等和困难示例

### 多样性检查清单

对于通用助手：
- 开放式问题（20%）
- 事实问答（20%）
- 创意写作（10%）
- 代码生成（15%）
- 推理和数学（15%）
- 摘要（10%）
- 带约束的指令遵循（10%）

根据领域特定模型调整百分比。编程助手可能分配 60% 给代码生成、20% 给代码解释。

## 第 2 步：数据格式

### Alpaca 格式（单轮）

```json
{
  "instruction": "写一个在 Python 中反转字符串的函数。",
  "input": "",
  "output": "def reverse_string(s):\n    return s[::-1]"
}
```

适用场景：单轮任务、简单的指令-响应对、快速原型开发。

### ShareGPT 格式（多轮）

```json
{
  "conversations": [
    {"from": "system", "value": "你是一个 Python 专家。"},
    {"from": "human", "value": "如何反转字符串？"},
    {"from": "gpt", "value": "使用切片：s[::-1]"},
    {"from": "human", "value": "那列表呢？"},
    {"from": "gpt", "value": "同样语法有效：my_list[::-1]"}
  ]
}
```

适用场景：对话应用，多轮上下文重要。

### ChatML 格式（带特殊 token）

```
<|im_start|>system
你是一个 Python 专家。<|im_end|>
<|im_start|>user
如何反转字符串？<|im_end|>
<|im_start|>assistant
使用切片：s[::-1]<|im_end|>
```

适用场景：目标模型原生使用 ChatML（Qwen、Yi）。

## 第 3 步：质量标准

### 每个示例的检查

1. **响应相关性**：响应是否真正回答了指令？
2. **事实准确性**：所有声明是否可验证且正确？
3. **完整性**：响应是否完整地回答了指令？
4. **简洁性**：能否用更少的词传达同样的信息？
5. **格式一致性**：响应是否遵循预期的风格？

### 危险信号（拒绝该示例）

- 响应自相矛盾
- 响应包含有害内容且未拒绝
- 响应产生幻觉事实或引用
- 指令模糊且响应未澄清
- 响应只是对指令的重新表述

### 数据集级别检查

- 来自任何单一来源/模板的示例不超过 5%
- 至少 80% 的响应 token 有意义（非填充内容）
- 平均响应长度为 50-200 token（避免过短或过长）
- 系统提示多样性：至少包含 10 个不同的系统提示

## 第 4 步：训练配置

| 参数 | 推荐范围 | 备注 |
|-----------|------------------|-------|
| 学习率 | 1e-5 到 5e-5 | 更大模型使用更低值（70B 用 1e-5，7B 用 5e-5） |
| 训练轮数 | 1-3 | 监控验证损失，在首次增加时停止 |
| 批处理大小 | 32-128 | 如果 GPU 受限，使用梯度累积扩展 |
| 预热 | 0-5% 的步数 | 不如预训练关键 |
| 权重衰减 | 0.0-0.1 | 短微调运行可选 |
| 损失掩码 | 仅响应 token | 屏蔽指令和系统提示 token |
| 预训练数据混合 | 2-5% | 混入原始文本以防止灾难性遗忘 |

## 第 5 步：评估协议

训练后，评估：

1. **指令遵循率**：模型产生相关、完整响应的测试提示百分比
2. **遗忘分数**：与基础模型相比在保留的通用文本语料库上的困惑度
3. **格式合规性**：遵循预期聊天格式的响应百分比
4. **MT-Bench 或 AlpacaEval**：指令微调模型的标准基准
5. **领域特定评估**：为目标能力定制的评估

### 警告信号

- 第 1 轮后验证损失增加：过拟合，减少轮次或增加数据
- 遗忘分数增加 > 15%：学习率过高或轮次过多
- 模型逐字复述训练示例：严重过拟合，需要更多样化的数据
- 模型拒绝良性指令：在安全数据上过训练，重新平衡数据集
