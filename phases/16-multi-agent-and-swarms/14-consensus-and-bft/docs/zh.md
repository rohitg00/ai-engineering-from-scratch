# 智能体的共识与拜占庭容错

> 经典分布式系统的 BFT 与随机的 LLM 相遇。在 2025-2026 年，三个研究方向涌现出来：**CP-WBFT**（arXiv:2511.10400）通过置信度探测对每个投票进行加权；**DecentLLMs**（arXiv:2507.14928）采用无领导者方式，由工作节点并行提出方案并取几何中位数聚合；**WBFT**（arXiv:2505.05103）将加权投票与层次结构聚类相结合，将节点分为核心节点和边缘节点。"Can AI Agents Agree?"（arXiv:2603.01213）给出的诚实实证结果是：即使标量共识在今天也很脆弱——单个欺骗性智能体就能破坏 Mixture-of-Agents。BFT 是必要的，但并不充分。本课程构建一个最小化的 BFT 协议，注入三种针对智能体的攻击（拜占庭谎言、谄媚性从众、相关性错误单一文化），并测量每种共识变体的应对能力。

**类型：** 学习 + 构建
**语言：** Python（标准库）
**前置知识：** 阶段 16·07（思维社会与辩论），阶段 16·13（共享内存）
**时长：** 约 75 分钟

## 问题

你有 N 个 LLM 智能体，每个都产生一个答案。它们存在分歧。多数投票选择了错误的答案，因为其中两个智能体是相关的（相同的基座模型、相同的训练数据、相同的失败模式）。第三个智能体恰好以另一种方式出错——因此多数是一个虚假的多数。

现在加入一个欺骗性智能体：它故意说谎。或者一个谄媚性智能体：它同意最后发言者的观点。在经典 BFT 中，假设拜占庭节点占比 `f < n/3`，且行为任意。2026 年的现实是：LLM 节点即使诚实也是随机的，跨模型之间存在相关性，并且会相互影响输出。你不能将它们视为独立的伯努利投票者。

经典 BFT（PBFT，1999）没有错——它是不完备的。它能处理任意比特翻转，但无法处理"三个诚实智能体因共享训练数据而产生相同的幻觉"这种情况。本课程从 PBFT 的基础出发，并在其上分层引入三种 2025-2026 的改进方案。

## 概念

### 经典 BFT 给你什么

实用拜占庭容错（Castro & Liskov，OSDI 1999）容忍 `f < n/3` 的拜占庭节点。协议包含三个阶段（预准备、准备、提交）和两个原语（签名消息、仲裁证书）。在 `n >= 3f + 1` 个诚实或恶意节点之间对单个值达成共识。

这些保证很强大，但假定：

1. **故障独立。** 拜占庭节点之间不协作。
2. **诚实节点是真正诚实的。** 诚实输出的正确性不是问题；协议只对齐分歧。
3. **问题存在真实答案。** 对一个错误事实达成共识仍然是共识。

LLM 智能体违反了所有三点。两个运行相同基座模型的智能体共享故障。一个"诚实"的 LLM 仍然会产生幻觉。而在有歧义的问题上，"真相"就是智能体决定的结果——没有外部预言机。

### 三种特定于 LLM 的攻击

**拜占庭谎言。** 一个智能体故意输出错误的答案。如果 `f < n/3`，经典 BFT 可以处理这种情况。

**谄媚性从众。** 一个智能体在投票前读取其他智能体的答案，并附和最后发言者的观点。这不是恶意的，但与最大声音相关。经典 BFT 无法阻止这种行为，因为该智能体通过了所有签名检查。

**相关性错误单一文化。** 三个智能体共享一个基座模型。它们对同一个错误答案产生了幻觉。多数是错误的。经典 BFT 无济于事，因为三个智能体"诚实"地达成了一致。

### 2025-2026 的应对方案

**CP-WBFT**（arXiv:2511.10400）——置信度探测加权 BFT。每个投票者为其答案附带一个置信度探测（自报概率，或由独立的校准模型预测）。投票权重随置信度缩放。在完全图上报告了 +85.71% 的 BFT 改进。缓解对象：谄媚性从众（从众的智能体对其主动给出的立场往往置信度较低）。

**DecentLLMs**（arXiv:2507.14928）——无领导者。工作智能体并行提出方案，评估智能体对提案打分，最终答案是打分后位置的几何中位数。在 `f < n/2` 时具有鲁棒性。缓解对象：拜占庭谎言和相关性错误（几何中位数对异常值具有鲁棒性，能向密集聚类靠拢，而非向有模型偏差的平均值靠拢）。

**WBFT**（arXiv:2505.05103）——带层次结构聚类的加权 BFT。投票权重由响应质量加上从历史学习到的信任分数分配。将智能体分为核心和边缘两类；核心智能体必须先达成共识，边缘智能体随后跟随。缓解对象：可扩展性（核心共识规模小且速度快）以及部分缓解单一文化（可以选择具有多样性的核心节点）。

### 实证："Can AI Agents Agree?"（arXiv:2603.01213）

该论文跨多个前沿模型测量了标量共识（LLM 智能体对单个数值达成一致）。结果令人不安：

- 即使没有对手，LLM 智能体在许多基准测试中对标量问题的分歧率超过 30%。
- 单个采用欺骗性角色的智能体可以将 Mixture-of-Agents 共识拉离诚实基线 40 个百分点以上。
- 分歧率与模型多样性相关——异构集成比同质集成分歧更大（好：错误不相关），但漂移也更慢（坏：达成共识所需时间更长）。

要点：BFT 为你提供了对齐输出的机制，但它不能告诉你对齐后的输出是否正确。需要结合验证（阶段 16·08 角色专业化）、多样性（阶段 16·15 辩论变体）和评估智能体（阶段 16·24 基准测试）。

### 核心协议，精简版

适用于 LLM 智能体的最小化 BFT 轮次：

```
1. 任务到达；每个智能体 i 产生答案 a_i
2. 每个智能体附带置信度探测 c_i ∈ [0, 1]
3. 聚合器从所有 n 个智能体收集 (a_i, c_i)
4. 聚合器按语义聚类分组（等价答案）
5. 聚合器计算每个聚类 C 的权重：
     w(C) = sum_{i ∈ C} c_i
6. 胜出者 = 权重最大的聚类，如果 max > threshold * sum(c_i)
   否则：重试或升级
7. 少数聚类连同来源记录被记录下来，供事后审计
```

语义聚类步骤是 LLM 特有的变化。两个答案"研究报告为 4.2%"和"4.2% 的改进"是同一个聚类。简单的字符串相等检查会漏掉这一点。在生产环境中，使用廉价的嵌入模型或显式的规范化处理。

### 阈值调优

`threshold` 参数决定何时接受、何时重试。太低：你会接受弱多数。太高：你永远不会接受任何东西。经验范围：对于 `n=5-7` 个智能体为 0.5-0.67，对于更小的 `n` 则更高。低于阈值时，升级到人类或切换到不同的智能体集成。

### 共识无济于事的场景

- **有歧义的问题。** 如果问题没有真实答案，共识就是一种意见。如实称呼它。
- **复合问题。** "写代码并解释它"——两个答案。分别对每个进行投票。
- **对抗性多轮。** 如果智能体可以观察前几轮并模仿（Du 2023 辩论），无论真相如何，它们都会开始彼此附和。限制轮次（通常 2-3 轮）。

## 构建

`code/main.py` 实现了：

- `AgentVoter`——一个基于脚本的策略，带有（答案，置信度）。
- `MajorityVote`——经典多数票。
- `CPWBFT`——带语义聚类的置信度加权投票。
- `DecentLLMs`——对打分提案进行几何中位数聚合。
- `Scenario`——在三种攻击模式下运行每个聚合器。

实现的攻击模式：

1. `byzantine`：一个智能体以高置信度说谎。
2. `sycophancy`：一个智能体复制它看到的第一个答案，并附带匹配的置信度。
3. `monoculture`：三个智能体共享一个错误答案（相关性错误），置信度为中等。

运行：

```
python3 code/main.py
```

预期输出：一个（攻击，聚合器）-> 最终答案的表格，正确答案被高亮显示。多数票在单一文化场景下失败。CPWBFT 的置信度加权缓解了谄媚性从众。DecentLLMs 的几何中位数在单一文化占比不到一半时向诚实聚类靠拢。

## 使用

`outputs/skill-consensus-designer.md` 为多智能体集成设计了一个共识协议：聚类方法、加权方式、阈值以及低于阈值时的升级策略。

## 交付

在交付任何共识机制之前：

- **至少用上述三种攻击模式进行测试。** 你的协议应可预测地失败，而非静默失败。
- **记录每一个少数聚类及其来源。** 少数聚类是发现相关性错误的早期预警系统。
- **强制执行有界轮次。** 不要"一直辩论直到达成一致"——这会奖励谄媚性从众。
- **将一致性与正确性分离。** 共识输出发送给验证器；验证器独立于集成。
- **监控共识率。** 急剧上升意味着从众偏差；急剧下降意味着模型漂移。

## 练习

1. 运行 `code/main.py`。确认多数票在单一文化攻击下失败，而 CPWBFT 在单一文化置信度低于 0.7 时能部分缓解。
2. 添加第四种攻击模式：**静默弃权**——一个智能体拒绝回答（"我不知道"）。每个聚合器应如何处理弃权？实现你的选择。
3. 将语义聚类从字符串规范化替换为嵌入相似度（使用任何开源嵌入模型）。谄媚性从众攻击会发生什么？
4. 阅读 CP-WBFT（arXiv:2511.10400）。实现置信度探测校准步骤（一个独立的校准模型检查每个智能体自报的置信度）。测量在单一文化场景下的准确率提升。
5. 阅读 "Can AI Agents Agree?"（arXiv:2603.01213）。复现一个简化的标量共识实验：三个智能体、一个标量问题、欺骗性角色提示。CPWBFT 或 DecentLLMs 能否捕获它？

## 关键术语

| 术语 | 人们说的意思 | 实际含义 |
|------|-------------|---------|
| BFT | "拜占庭容错" | Castro-Liskov 1999 协议，在 `f < n/3` 的任意故障下达成共识。 |
| Byzantine | "任何不良行为" | 一个节点可以说谎、丢弃消息、静默失败——任何除了安全崩溃之外的行为。 |
| 置信度探测 | "你有多确定？" | 自报或校准器预测的概率，附在投票上。 |
| 语义聚类 | "相同答案，不同说法" | 在计票前对等价答案进行分组。 |
| 几何中位数 | "鲁棒中心" | 使到样本点距离之和最小的点。对异常值具有鲁棒性，与均值不同。 |
| 单一文化 | "相同模型，相同失败" | 智能体共享训练数据或基座模型时产生的相关性错误。 |
| 谄媚性从众 | "附和最大声音" | 智能体的投票偏向于先发言/最大声者的观点。 |
| 核心/边缘 | "层次化 BFT" | WBFT 的划分：小型核心共识先达成，边缘节点随后跟随。限制延迟。 |

## 延伸阅读

- [Castro & Liskov — Practical Byzantine Fault Tolerance (OSDI 1999)](https://pmg.csail.mit.edu/papers/osdi99.pdf) — 基础论文
- [CP-WBFT — Confidence-Probe Weighted BFT](https://arxiv.org/abs/2511.10400) — 按置信度进行投票加权
- [DecentLLMs — leaderless multi-agent consensus](https://arxiv.org/abs/2507.14928) — 几何中位数聚合
- [WBFT — Weighted BFT with Hierarchical Structure Clustering](https://arxiv.org/abs/2505.05103) — 核心/边缘划分以限制延迟
- [Can AI Agents Agree?](https://arxiv.org/abs/2603.01213) — 标量共识的脆弱性与欺骗性角色攻击
